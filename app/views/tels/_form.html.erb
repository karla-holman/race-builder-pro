<%= form_for(@tel) do |f| %>
  <% if @tel.errors.any? %>
  <div class="row">
    <div class="col-xs-12">
      <div id="error_explanation">
        <h2><%= pluralize(@horse.errors.count, "error") %> prohibited this tel from being saved:</h2>
        <ul>
        <% @horse.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    </div>
    <div class="col-xs-12 col-md-6">  
      <div class="form-group">
        <%= f.label :section %>
        <%= f.select :section, options_for_select([['Protocol'], ['Alternate']]) %>
      </div>
      <div class="form-group">
      	<%= f.hidden_field :race_id, :value => @race_id %>
      	<%= f.hidden_field :day, :value => @day %>
        <%= f.submit :class => 'btn btn-success btn-sm'%>
        <%= link_to 'Back', tels_sunday_path, :class => 'btn btn-danger btn-sm' %>
      </div>
    </div>
  </div>
<% end %>
