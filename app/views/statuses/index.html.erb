<div class="row">
  <div class="col-xs-12">
    <h2>RBP Statuses</h2>
    <%= link_to 'New Status', new_status_path, :class => 'btn btn-primary btn-small' %>
  </div>
  <div class="col-xs-12">
    <div class="grid simple">
      <div class="grid-body no-border">
        <div class="row">
          <div class="col-xs-12">
            <table class="genericTable table table-condensed table-hover">
              <thead>
                <tr>
                  <th>Name</th>
                  <th colspan="1"></th>
                </tr>
              </thead>
              <tbody>
                <% @statuses.each do |status| %>
                  <tr>
                    <td><%= status.name %></td>
                    <td>
                      <%= link_to 'Edit', edit_status_path(status), :class => 'btn btn-warning btn-mini' %>
                      <%= link_to 'Destroy', status, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger btn-mini' %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>