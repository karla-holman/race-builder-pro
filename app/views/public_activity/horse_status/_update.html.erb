<% @horse = Horse.find_by_id(activity.trackable.horse_id) %>
<% @status = Status.find_by_id(activity.trackable.status_id) %>

Updated horse
<% if activity.trackable %>
	<% if @horse %>
		<%= link_to @horse.name, @horse %>'s
	<% else %>
		which has been removed
	<% end %>
	status to
	<% if @status %>
		<%= link_to @status.name, @status %>
	<% else %>
		a status which has been removed
	<% end %>
<% else %>
  status which has since been removed 
<% end %>