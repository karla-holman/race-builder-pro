updated <%= Horse.find(activity.trackable.horse_id).name %>'s status to
<% if activity.trackable %>
  <%= link_to Status.find(activity.trackable.status_id).name, activity.trackable %>
<% else %>
  which has since been removed 
<% end %>