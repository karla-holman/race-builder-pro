<%= form_for(@horse, remote: true) do |f| %>
  <% if @horse.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@horse.errors.count, "error") %> prohibited this horse status from being saved:</h2>
    <ul>
    <% @horse.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <div class="form-group attribute">
    <% if current_user.admin? %>
      <%= f.text_field :last_claiming_price, :autofocus => true, class: 'form-control' %>
      <%= f.submit :class => 'btn btn-success btn-sm'%>
    <% else %>
      <%= f.text_field :last_claiming_price, :value => number_to_currency(f.object.last_claiming_price, :precision => 0), :autofocus => true, :disabled => true, class: 'form-control' %>
    <% end %>
  </div>
<% end %>