<%= form_for(@horse, remote: true) do |f| %>
  <% if @current_status.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@current_status.errors.count, "error") %> prohibited this horse status from being saved:</h2>
    <ul>
    <% @current_status.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <div class="form-group horse-attribute">
    <%= f.text_field :last_win, :autofocus => true, class: 'date-picker form-control' %>
  </div>
<% end %>