<%= form_for(@horse, remote: true) do |f| %>
  <% if @horse.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@horse.count, "error") %> prohibited this horse status from being saved:</h2>
    <ul>
    <% @horse.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <div class="form-group attribute">
    <%= f.text_field :last_win, :autofocus => true, class: 'datepicker form-control' %>
  </div>
<% end %>