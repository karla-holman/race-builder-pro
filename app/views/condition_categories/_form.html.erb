<%= form_for(@condition_category) do |f| %>
  <div class="row">
    <div class="col-xs-12">
        <% if @condition_category.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@condition_category.errors.count, "error") %> prohibited this condition_category from being saved:</h2>
            <ul>
              <% @condition_category.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
    </div>
    <div class="col-xs-12 col-md-6">  
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, :autofocus => true, class: 'form-control'%>
      </div>
      <div class="form-group">
        <%= f.submit :class => 'btn btn-success btn-sm'%>
        <%= link_to 'Back', condition_categories_path, :class => 'btn btn-danger btn-sm' %>
      </div>
    </div>
  </div>
<% end %>

